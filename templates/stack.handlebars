<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>technology stack page</title>
  <meta name="viewport"
        content="width=device-width, initial-scale=1">
  <style>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .stack-list {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-evenly;
    }

    .stack-item {
      display: flex;
      margin-right: 1em;
      flex-direction: column;
      align-items: center;
    }

    .best-size {
      width: 100px;
      height: 100px;
    }

    .best-size>* {
      width: 100%;
      height: 100%;
    }
  </style>
</head>

<body>
  <h1>Stack List</h1>
  <div class="stack-list">
    {{#each dependencyIcons}}
    <div class="stack-item">
      <div class="best-size">
        {{{this.icon.svg}}}
      </div>
      <div class="content">
        <h1>{{this.depName}}</h1>
        {{!-- <p>Get safety and consistency with real-time monitoring and automatic updates for npm dependencies</p> --}}
      </div>
    </div>
    {{/each}}
  </div>
</body>

</html>
